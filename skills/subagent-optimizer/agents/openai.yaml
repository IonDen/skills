interface:
  display_name: "Subagent Optimizer"
  short_description: "Audit Claude Code subagents and Codex custom agents"
  default_prompt: "Use $subagent-optimizer to audit my Claude Code subagents and Codex custom agents for token cost, tool allowlists, Codex key problems, model and effort."
policy:
  allow_implicit_invocation: true

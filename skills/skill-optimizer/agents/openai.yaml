interface:
  display_name: "Skill Optimizer"
  short_description: "Cut a skill's token cost without losing an instruction"
  default_prompt: "Use $skill-optimizer to shrink a SKILL.md without losing any instruction."
policy:
  allow_implicit_invocation: true
